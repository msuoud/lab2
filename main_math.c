#include <stdio.h>
// your include

int main() {
  double a, b;
  int flag;

  // your declaration

  printf("Enter the value of a: ");
  scanf("%lf", &a);

  printf("Enter the value of b: ");
  scanf("%lf", &b);

  printf("Press 0 to compute as double, 1 as int: ");
  scanf("%d", &flag);

  // your code to compute either as integer or double

  return 0;
}
